<div class="userIn-class">
    <div>
        <img src="{{userIn.image}}"
    </div>

    <div class="user-info">
        <h1>Name/Company: {{userIn.name}}</h1>
        <h2>Email: {{userIn.email}}</h2>
        <h2>Password: {{userIn.password}}</h2>
        <a href="user-edit.hbs">Edit profile</a>
    </div>
</div>